import { QiezziDicPipe } from './qiezzi-dic.pipe';

describe('QiezziDicPipe', () => {
  it('create an instance', () => {
    const pipe = new QiezziDicPipe();
    expect(pipe).toBeTruthy();
  });
});
